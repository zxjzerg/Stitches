name: data
description: data layer of stitches
publish_to: none

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  ## 本地依赖
  # 基础组件
  framework:
    path: ../framework
  # 基础组件，当集成在Native App中使用
  framework_integrated:
    path: ../framework_integrated
  core:
    path: ../core

  ## 外部库依赖
  get_it: ^7.2.0